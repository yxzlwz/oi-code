#include<bits/stdc++.h>
using namespace std;

const int K = 200001;

int dp[11][K];

int n, m;
int main(){
    cin >> n >> m;
    while(n || m){
        memset(dp, 0, sizeof(dp));
        
        cin >> n >> m;
    }
}